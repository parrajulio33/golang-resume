{
  "name": "resume-app",
  "version": "1.0.0",
  "description": "Julio's Resume App",
  "scripts": {
    "build:css": "tailwindcss -i ./static/input.css -o ./static/output.css --minify",
    "watch:css": "tailwindcss -i ./static/input.css -o ./static/output.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}